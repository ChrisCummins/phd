#!/usr/bin/env bash
set -eu

ROOT="$(pwd)"

rm -rf "$ROOT/logs"
mkdir -p "$ROOT/logs/good"

cd bin
if [[ $DEVTYPE == "CPU" ]];then
    ./run_ocl_cpu.sh
    cp log/*.cpu.out "$ROOT/logs/good"
else
    ./run_ocl_gpu.sh
    cp log/*.gpu.out "$ROOT/logs/good"
fi
